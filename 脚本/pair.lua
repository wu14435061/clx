function Ğ¡ºÅÅÅĞò(trueKeys)
	local data = {}
	for _, key in pairs(trueKeys) do
		if key == "faroz3" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "faroz4" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "faroz5" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "faroz6" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "faroz7" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "faroz8" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "faroz9" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "faroz10" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "faroz11" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "faroza" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "farozb" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "farozc" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "farozd" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "faroze" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "farozf" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "farozg" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "farozh" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "farozi" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "farozj" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "farozk" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "farozl" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "farozq" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "farozr" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "farozt" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "farozu" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "farozw" then
			table.insert(data, key)
		end
	end
	for _, key in pairs(trueKeys) do
		if key == "farozy" then
			table.insert(data, key)
		end
	end
	return data
end
